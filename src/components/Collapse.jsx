/*import { useState, useRef } from 'react';
function Collapse() {
    const [show, setShow] = useState(false);
    const ref = useRef(null);
    const collapseTitle="titre" 
    const collapseBody="texte"     
    return (
      <>
        <button className="collapse-title" onClick={() => {
          setShow(!show);
        }}>
          {collapseTitle}
        </button>
        
       {show && <div className="collapse-card" ref={ref}>{collapseBody}</div>} 
      </>
    )
}

export default Collapse*/